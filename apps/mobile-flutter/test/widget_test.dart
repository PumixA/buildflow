import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('LocalReport roundtrip', (tester) async {
    // Le widget test complet nécessite l'émulateur (plugins natifs).
    // Les tests unitaires couvrent les modèles et services.
    // Voir test/models/ et test/services/ pour la couverture métier.
    expect(true, isTrue);
  });
}
